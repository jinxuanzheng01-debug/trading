#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../yaml/bin.mjs" "$@"
else
  exec node  "$basedir/../../../yaml/bin.mjs" "$@"
fi
# cmd-shim-target=/Users/xuan/Documents/trading-agent/node_modules/.pnpm/vite@7.3.3_@types+node@20.19.41_jiti@2.7.0_lightningcss@1.32.0_terser@5.47.1_tsx@4.22.2_yaml@2.9.0/node_modules/yaml/bin.mjs
