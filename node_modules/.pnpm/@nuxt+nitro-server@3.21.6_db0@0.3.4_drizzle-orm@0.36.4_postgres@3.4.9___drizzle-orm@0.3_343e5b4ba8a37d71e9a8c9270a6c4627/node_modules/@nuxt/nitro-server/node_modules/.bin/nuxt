#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../nuxt/bin/nuxt.mjs" "$@"
else
  exec node  "$basedir/../../../../nuxt/bin/nuxt.mjs" "$@"
fi
# cmd-shim-target=/Users/xuan/Documents/trading-agent/node_modules/.pnpm/@nuxt+nitro-server@3.21.6_db0@0.3.4_drizzle-orm@0.36.4_postgres@3.4.9___drizzle-orm@0.3_343e5b4ba8a37d71e9a8c9270a6c4627/node_modules/nuxt/bin/nuxt.mjs
